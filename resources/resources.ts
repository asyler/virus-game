/**
 * Created by asyler on 12.01.17.
 */

interface Resources {
    strings: { [id: string] : any },
    fonts: { [id: string] : any }
}

let R : Resources = {
    strings: undefined,
    fonts: undefined
};